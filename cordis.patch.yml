# dsh-chat-ux 贡献的组合包层。
#
# 一个把这个包列进 `dsh.profile.bundles` 的 profile 会在所有更早的层之后应用这个 patch，
# 所以下面这一行就是加载器挂载的东西。这一行按包名解析，这也是为什么装好的副本启动的是它自己
# 构建出来的入口，而不是检出目录里的某个路径。
- insert:
    - id: dsh-chat-ux
      name: '@alm-allen/dsh-chat-ux'
